apiVersion: v1
kind: Service
metadata:
  name: pdf-server-service

spec:
  selector:
    app: pdf-server

  ports:
    - protocol: TCP
      port: 9999
      targetPort: 9999
      nodePort: 30082 #브라우저 접속 포트 (http://localhost:30082)
  type: ClusterIP
