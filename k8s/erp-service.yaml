apiVersion: v1
kind: Service
metadata:
  name: erp-server-service

spec:
  selector:
    app: erp-server

  ports:
    - protocol: TCP
      port: 9999
      targetPort: 9999
      nodePort: 30082 #브라우저 접속 포트 (http://localhost:30082)
  type: ClusterIP
