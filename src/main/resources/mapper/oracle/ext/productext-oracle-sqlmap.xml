<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.erp_server.ext.service.dao.oracle.ProductMapper">

    <resultMap id="ProductResultMap"
               type="org.example.erp_server.ext.dto.Product">

        <id property="productId" column="PRODUCT_ID"/>
        <result property="productName" column="PRODUCT_NAME"/>
        <result property="price" column="PRICE"/>
        <result property="stock" column="STOCK"/>
        <result property="productCode" column="PRODUCT_CODE"/>
        <result property="version" column="VERSION"/>
        <result property="deleted" column="DELETED"/>

    </resultMap>


    <!-- 전체 조회 -->
    <select id="findAll" resultMap="ProductResultMap">
        SELECT PRODUCT_ID,
               PRODUCT_NAME,
               PRICE,
               STOCK,
               PRODUCT_CODE,
               VERSION,
               DELETED
        FROM PRODUCT
    </select>


    <!-- ID 조회 -->
    <select id="findById"
            parameterType="long"
            resultMap="ProductResultMap">

        SELECT PRODUCT_ID,
               PRODUCT_NAME,
               PRICE,
               STOCK,
               PRODUCT_CODE,
               VERSION,
               DELETED
        FROM PRODUCT
        WHERE PRODUCT_ID = #{productId}
          AND DELETED = 'N'

    </select>

    <select id="findByIdIncludingDeleted"
            parameterType="long"
            resultMap="ProductResultMap">

        SELECT PRODUCT_ID,
               PRODUCT_NAME,
               PRICE,
               STOCK,
               PRODUCT_CODE,
               VERSION,
               DELETED
        FROM PRODUCT
        WHERE PRODUCT_ID = #{productId}

    </select>


    <!-- 이름 조회 -->
    <select id="findByName"
            parameterType="String"
            resultMap="ProductResultMap">

        SELECT PRODUCT_ID,
               PRODUCT_NAME,
               PRICE,
               STOCK,
               PRODUCT_CODE,
               VERSION,
               DELETED
        FROM PRODUCT
        WHERE PRODUCT_NAME = #{productname}
        and DELETED = 'N'

    </select>


    <!-- 상품 등록 -->
    <insert id="insert"
            parameterType="org.example.erp_server.ext.dto.Product">

        <selectKey
                keyProperty="productId"
                resultType="long"
                order="BEFORE">

            SELECT PRODUCT_SEQ.NEXTVAL
            FROM DUAL

        </selectKey>

        INSERT INTO PRODUCT (
        PRODUCT_ID,
        PRODUCT_NAME,
        PRICE,
        STOCK,
        PRODUCT_CODE,
        VERSION,
        DELETED
        )
        VALUES (
        #{productId},
        #{productName},
        #{price},
        #{stock},
        #{productCode},
        0,
        'N'
        )

    </insert>


    <!-- 상품 수정 -->
    <update id="update"
            parameterType="org.example.erp_server.ext.dto.Product">

        UPDATE PRODUCT
        SET PRODUCT_NAME = #{productName},
            PRICE = #{price},
            STOCK = #{stock},
            VERSION = VERSION + 1
        WHERE PRODUCT_ID = #{productId}
        AND VERSION = #{version}
        AND DELETED = 'N'

    </update>


    <!-- 상품 삭제 -->
    <update id="delete"
            parameterType="org.example.erp_server.ext.dto.Product">

        UPDATE PRODUCT
        SET DELETED = 'Y',
            VERSION = VERSION + 1
        WHERE PRODUCT_ID = #{productId}
          AND VERSION = #{version}
          AND DELETED = 'N'

    </update>


    <!-- Elasticsearch 초기 적재 -->
    <select id="findProductsForIndex"
            resultMap="ProductResultMap">

        SELECT PRODUCT_ID,
               PRODUCT_NAME,
               PRICE,
               STOCK,
               PRODUCT_CODE,
               VERSION,
               DELETED
        FROM PRODUCT
        WHERE PRODUCT_ID > #{lastId}
        AND DELETED = 'N'
        ORDER BY PRODUCT_ID
            FETCH NEXT #{size} ROWS ONLY

    </select>


    <!-- 상품 페이지 조회 -->
    <select id="findPage"
            resultMap="ProductResultMap">

        SELECT PRODUCT_ID,
               PRODUCT_NAME,
               PRICE,
               STOCK,
               PRODUCT_CODE,
               VERSION,
               DELETED
        FROM PRODUCT
        WHERE DELETED = 'N'
        ORDER BY PRODUCT_ID
        OFFSET #{offset} ROWS
            FETCH NEXT #{size} ROWS ONLY

    </select>


    <!-- 전체 상품 개수 -->
    <select id="countProducts"
            resultType="long">

        SELECT COUNT(*)
        FROM PRODUCT
        WHERE DELETED = 'N'

    </select>

</mapper>
